# Phase 9A ablation scaffold preset: baseline control branch.
# Use as an override together with an existing local/server base config.

experiment:
  name: phase9a-baseline-scaffold

training:
  ablation:
    enabled: true
    variant: baseline
    allow_scaffold_only: true
    notes: "Phase 9A scaffold-only branch for baseline control metadata."

logging:
  run_name: phase9a-baseline-scaffold
