# Data Inventory (Compatibility Page)

Canonical data split mapping and usage details now live in:

- `docs/reference/data_configuration.md`

Reporting output policy and generation commands now live in:

- `docs/reference/reporting_and_artifacts.md`

Generated artifacts are written under `docs/generated/` and are not intended to be versioned.

Start at:

- `docs/README.md`
